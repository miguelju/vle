{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git push:*)",
            "prompt": "This hook guards pushes to the vle repository only. FIRST identify which repository the push targets: read the command from $ARGUMENTS (hook input JSON, field tool_input.command). If it contains `git -C <dir>` or a leading `cd <dir> &&`, the target repo is <dir>; otherwise it is the current working directory. Resolve that repo's root and check whether it contains ALL THREE of TODO.md, ROADMAP.md, and MODERNIZATION_PLAN.md at its root. If it does NOT contain all three, this is not the vle repo — allow the push immediately and perform no further checks. Otherwise, verify that TODO.md and ROADMAP.md are in sync and updated for any work done. Check:\n1. Every milestone in ROADMAP.md has a corresponding section in TODO.md (and vice versa), with matching milestone numbers and names.\n2. Tasks that have been completed (files exist, code written) are checked off [x] in both files.\n3. Phase pointers in ROADMAP.md (*Phase N of MODERNIZATION_PLAN.md*) match the phase headers in MODERNIZATION_PLAN.md.\n4. The summary table in TODO.md matches the current milestone list.\n5. No stale milestone references (e.g., old fractional milestone numbers like 1.5 or 2.5) exist.\n\nIf everything is in sync, allow the push. If anything is out of sync, block the push and explain what needs to be fixed.",
            "timeout": 120,
            "statusMessage": "Verifying TODO.md and ROADMAP.md are in sync..."
          }
        ]
      }
    ]
  }
}
