# JVM memory for the Gradle daemon (the Kotlin compiler runs inside it).
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# AndroidX (the modern Android support libraries) — required by AGP 8+.
android.useAndroidX=true
